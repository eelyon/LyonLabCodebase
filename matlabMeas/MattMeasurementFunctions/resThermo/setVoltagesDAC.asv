%% Function to bulk set voltages on the DAC

volts = [-2.5,...  %DAC 1
        2,...   %DAC 2
        1,...   %DAC 3
        1,...   %DAC 4
        1,...   %DAC 5
        0,...   %DAC 6
        0.5,...   %DAC 7
        0];     %DAC 8

for i = 1:length(volts)
    setVal(DAC,i,volts(i));
end